当前“模拟飞行员工作台”通过训练平台和 BlueSky Adapter，正式支持 **6 种文本指令**：

`HDG`、`ALT`、`SPD`、`MACH`、`DCT`、`RTE`。

注意：BlueSky 核心本身还有大量原生命令，但当前工作台不会把任意 BlueSky 命令直接透传，只接受下面六种格式。

## 一、基本操作方法

1. 确认顶部引擎指示为绿色，提示“BlueSky 已连接”。
2. 创建并选择一架航空器。
3. 在页面底部的指令框输入指令。
4. 按不同快捷键提交：

| 操作 | 调度方式 | 效果 |
| --- | --- | --- |
| `Enter` | 插到当前之后 | 同通道没有执行中指令时立即执行；否则插在当前指令之后 |
| `Shift+Enter` | 追加队尾 | 加到同一控制通道等待队列末尾 |
| `Ctrl+Enter` | 立即执行 | 覆盖当前同通道指令，并取消该通道原有等待指令 |

输入时不需要写航空器呼号。例如选择 `CCA3582` 后，只输入：

```text
HDG 090
```

不要输入：

```text
CCA3582 HDG 090
```

系统会自动去除首尾空格、合并连续空格并转为大写。

## 二、支持的指令

### 1. HDG——指定航向

格式：

```text
HDG <航向>
```

示例：

```text
HDG 090
HDG 270
HDG 360
```

规则：

- 单位为度。
- 接受范围为 `0～360`。
- 可以输入小数，如 `HDG 92.5`。
- `360` 在引擎执行时等价于 `0`。
- 指令完成条件：实际航向与目标航向的圆周最短误差不超过 `2°`。
- `HDG` 属于横向通道，会与 `DCT`、`RTE` 相互排队或覆盖。
- 执行 `HDG` 会退出正在跟踪的 `DCT` 直飞状态。

当前不支持：

```text
HDG L 090
HDG R 270
TURN LEFT 090
```

也就是暂不支持指定左转或右转方向，引擎自行采用正常转向逻辑。

### 2. ALT——指定高度

格式：

```text
ALT <高度>
ALT <高度> VS <升降率>
```

示例：

```text
ALT 12000
ALT 30000
ALT 12000 VS 1000
ALT 6000 VS 1500
```

规则：

- 高度单位固定为英尺 `ft`。
- 高度必须大于或等于 `0`。
- `VS` 单位固定为英尺/分钟 `ft/min`。
- `VS` 输入的是升降率大小，方向由目标高度和航空器执行时的实际高度自动决定。
- 即使输入负值，当前解析器也会取绝对值，再自动决定爬升或下降方向。
- 排队后的 ALT 指令真正开始执行时，会根据当时的最新高度重新确定 VS 正负方向。
- 不写 `VS` 时，由 BlueSky 自动驾驶自行决定升降过程。
- 对具有导入性能数据的机型，会检查目标高度是否超过升限。
- 显式 VS 超过机型当前高度下的最大升降率时，会按性能限制自动限幅。
- 指令完成条件：实际高度与目标高度误差不超过 `100 ft`。
- `ALT` 使用独立的垂直通道，可以与航向和速度指令同时执行。

当前不要使用单位后缀：

```text
ALT FL200
ALT 12000FT
ALT 12000 VS 1000FPM
```

这些格式当前都会被拒绝。

### 3. SPD——指定指示空速

格式：

```text
SPD <速度>
```

示例：

```text
SPD 220
SPD 250
SPD 280
```

规则：

- 单位固定为节 `kt`。
- 当前含义是 CAS/指示空速目标。
- 数值必须大于 `0`。
- 对具有导入性能数据的机型，会按航空器当前高度检查允许的巡航速度包线。
- 超出包线会拒绝指令，并返回类似“允许 196–310 kt”的实际范围。
- 指令完成条件：实际速度与目标速度误差不超过 `5 kt`。
- `SPD` 和 `MACH` 共用速度通道，不能同时作为两个独立速度目标执行。

当前不要使用：

```text
SPD 250KT
SPD 500KPH
SPEED 250
```

### 4. MACH——指定马赫数

格式：

```text
MACH <马赫数>
```

示例：

```text
MACH 0.72
MACH 0.78
MACH 0.82
```

规则：

- 取值必须满足 `0 < MACH < 1`。
- 必须写成普通小数形式。
- 指令完成条件：实际马赫数与目标值误差不超过 `0.01`。
- 与 `SPD` 共用速度通道。
- 当前不使用导入性能表中的 Mach 包线做额外校验，但 BlueSky/OpenAP 自身仍会施加飞机性能限制。

不支持：

```text
MACH M.78
MACH .78
SPD M.78
```

建议始终输入：

```text
MACH 0.78
```

### 5. DCT——直飞当前航路中的点

格式：

```text
DCT <航路点>
```

示例：

```text
DCT CEN
DCT SASAN
DCT ZBAA
```

规则：

- 目标代码不区分大小写。
- 目标点必须已经存在于该航空器的当前航路中。
- 不能用 DCT 直接飞向当前航路之外的任意导航点。
- 执行后会启用 LNAV，并把指定点设为活动航路点。
- DCT 不会在引擎“接受指令”时立即完成。
- 航空器通过目标点并切换到后续航段后，DCT 才进入 `COMPLETED`。
- DCT 属于横向通道，会与 `HDG`、`RTE` 相互排队或覆盖。

不支持别名：

```text
DIRECT CEN
DIRTO CEN
DIRECTTO CEN
```

当前工作台只接受 `DCT`。

### 6. RTE——完整替换未飞航路

格式：

```text
RTE <航路点1> <航路点2> ... <落地机场>
```

示例，航空器落地机场为 `ZBAA`：

```text
RTE CEN CON ZBAA
RTE ZSSS CEN CON ZBAA
RTE ZBAA
```

规则：

- 至少提供一个点。
- 所有代码都必须是 BlueSky 导航库中的有效航路点或机场。
- 最后一个代码必须与航空器原有落地机场完全一致。
- RTE 不能借此改变航空器的落地机场。
- 执行后会清除原航路并按输入顺序建立新航路。
- 新航路第一个点立即成为活动点，并启用 LNAV。
- 航路更新是原子操作：任意点无效或加入失败时，会恢复原航路。
- Adapter 返回匹配的航路激活回执后，指令进入 `COMPLETED`。
- RTE 属于横向通道，会覆盖直飞状态。

错误示例：

```text
RTE CEN CON
```

如果航空器目的地是 `ZBAA`，上面的最后一点不是 `ZBAA`，因此会被拒绝。

```text
RTE CEN UNKNOWN ZBAA
```

如果 `UNKNOWN` 不在导航库中，整条航路都会被拒绝，原航路保持不变。

## 三、三个独立控制通道

当前系统把指令分成三个通道：

| 控制通道 | 指令 |
| --- | --- |
| 横向通道 | `HDG`、`DCT`、`RTE` |
| 垂直通道 | `ALT` |
| 速度通道 | `SPD`、`MACH` |

不同通道可以并行执行。例如下面三条可以同时处于执行中：

```text
HDG 090
ALT 12000 VS 1000
SPD 250
```

同一通道则按提交快捷键排队或覆盖。例如：

1. 输入 `HDG 090`，按 `Enter`：立即执行。
2. 输入 `HDG 180`，按 `Shift+Enter`：追加到横向队尾。
3. 输入 `DCT CEN`，按 `Enter`：插入到 `HDG 090` 后、`HDG 180` 前。
4. 输入 `HDG 270`，按 `Ctrl+Enter`：立即执行，并取消当前横向指令和横向等待队列。

垂直、速度通道不会因为横向覆盖而被取消。

## 四、指令状态

指令可能处于以下状态：

| 状态 | 含义 |
| --- | --- |
| `PENDING` | 正在等待同通道前序指令完成 |
| `EXECUTING` | 已被 Adapter 接受并正在执行 |
| `COMPLETED` | 已满足完成条件 |
| `FAILED` | 语义校验、性能校验或引擎执行失败 |
| `CANCELLED` | 被同通道的 `Ctrl+Enter` 指令覆盖取消 |

如果一条排队指令下发失败，它会标记为 `FAILED`；后续同通道指令会在下一次状态处理时继续尝试，不会永久堵塞整个队列。

## 五、开始、暂停和恢复

顶部按钮属于仿真生命周期控制，不是文本指令：

- `READY` 状态点击 `▶`：开始练习。
- `RUNNING` 状态点击 `Ⅱ`：暂停练习。
- `PAUSED` 状态点击 `▶`：恢复练习。

在暂停或尚未开始时可以设置控制目标，但航空器不会持续运动，指令通常也不会完成，直到练习运行。

## 六、当前明确不支持的文本指令

当前输入框不支持：

- 单独的 `VS`
- `LNAV`、`VNAV`
- `HOLD`、`ORBIT`
- `SQK`、`SQUAWK`
- `FSA`
- `HDG L/R`
- `RER`
- 航路外 DCT
- `DIRECT`、`SPEED` 等 BlueSky 原生命令别名
- 一条文本控制多架航空器
- 带 `FT`、`FPM`、`KT`、`FL` 等单位后缀的写法

## 七、通过 API 下发

接口：

```http
POST /api/v1/aircraft/{aircraftId}/instructions
Content-Type: application/json
```

示例：

```json
{
  "text": "ALT 12000 VS 1000",
  "insertion": "AFTER_CURRENT"
}
```

`insertion` 可取：

```text
AFTER_CURRENT
IMMEDIATE
APPEND
```

查询航空器的全部指令：

```http
GET /api/v1/aircraft/{aircraftId}/instructions
```

当前实现依据：

- [InstructionParser.java](D:/development/development/repo/bluesky/bluesky-master/training-platform/src/main/java/org/bluesky/training/instruction/InstructionParser.java:14)
- [InstructionService.java](D:/development/development/repo/bluesky/bluesky-master/training-platform/src/main/java/org/bluesky/training/instruction/InstructionService.java:43)
- [InstructionProgressService.java](D:/development/development/repo/bluesky/bluesky-master/training-platform/src/main/java/org/bluesky/training/instruction/InstructionProgressService.java:85)
- [engine.py](D:/development/development/repo/bluesky/bluesky-master/bluesky/plugins/training_adapter/engine.py:210)
- [commandKeys.ts](D:/development/development/repo/bluesky/bluesky-master/training-platform/frontend/src/commandKeys.ts:8)